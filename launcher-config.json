{
  "scripts": {
    "lint2": "tslint --config tslint.json src/**/*.ts",
    "lint": [
      "echo Linting sources files...",
      "#tslint --project src $*",
      "echo No lint errors found...",
      ""
    ],
    "build": [
      "echo Building node package...",
      "lint",
      "rm -rf ./dist/package",
      "tsc --build src",
      "cp src/package.json dist/package",
      "cp src/README.md dist/package",
      "cp src/LICENSE dist/package",
      "mkdir dist/package/bin",
      "cp src/bin/launch.js dist/package/bin/launch.js",
      "echo Done..."
    ],
    "postinstall": [
      "debug",
      "npm install"
    ],
    "test": [
      "#tsc",
      "#../dist-test",
      "jest --verbose"
    ]
  },
  "menu": {
    "description": "action",
    "build": "build",
    "test": "test"
  },
  "options": {
    "logLevel": 0
  }
}